
import React from "react";
import { Layout } from "@/components/layout/Layout";
import { SupportTicketEdit } from "@/components/support/SupportTicketEdit";

const SupportTicketEditPage = () => {
  return (
    <Layout>
      <SupportTicketEdit />
    </Layout>
  );
};

export default SupportTicketEditPage;
